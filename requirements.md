# 要件定義書（個人資産・カード管理アプリ）

| 項目 | 内容 |
|---|---|
| 文書名 | 要件定義書 |
| プロジェクト名 | 個人資産・カード管理アプリ（moneger） |
| 作成日 | 2026-06-01 |
| 最終更新日 | 2026-06-04 |
| 対象ユーザー | 作成者本人（自分用） |
| 対象OS | Windows |
| 開発言語 | Python |
| UIフレームワーク | PySide6（確定） |

---

## 1. 背景・目的

本アプリは、現金・銀行口座・クレジットカード・株式・暗号資産などの個人資産を一元管理し、資産・負債・収支・実現損益の状況を可視化することを目的とする。

特に、クレジットカード利用については、ユーザが用意したCSV等の明細をもとに、**請求月・引落月単位**で未引落額を把握し、引落口座との関係を含めて管理できることを重視する。

また、株式投資（日本株・米国株）および暗号資産については、**税額計算ではなく実現損益の把握**を主目的とする。税額は正確な算出対象とはせず、必要に応じて将来、ユーザ設定の税率に基づく参考表示に留める。

> 注: 本アプリは税務申告書の作成や提出を行うものではない。表示される損益・税額参考値は、取得データと設定条件に基づく目安であり、正確性・完全性・税務上の適合性を保証しない。

---

## 2. スコープ

### 2.1 スコープ内（本アプリで実現する）

- Windowsデスクトップアプリとして動作すること（Webアプリ不可）
- ローカル（オフライン）で資産・負債・収支を管理できること
- データの暗号化保存（PC盗難・バックアップ流出を想定）
- 楽天銀行／楽天カード／沖縄銀行のCSV等によるデータ取り込み
- 銀行・カード系APIは、利用可能で安全に運用できる場合に限り採用を検討すること
- 暗号資産については、API取得およびオンチェーンデータ取得を主要な同期方式として検討し、CSV取り込みも選択肢として残すこと
- 株式（日本株・米国株）および暗号資産の実現損益を集計・表示できること
  - 暗号資産の損益計算方式は**総平均法**を採用する
  - 米国株等の外貨建て資産について、為替換算ルールを選択可能とする（→ 10.7）
- 複数の取得経路（CSV/API/オンチェーン）から同一または関連する取引が入る可能性を考慮し、重複・競合・二重計上を防止できる設計とすること
- BS/PL等の会計的な可視化と、簡易UI（家計簿的UI）の両立
- 将来、資産種類・取得元・同期方式を追加可能な拡張性

### 2.2 税額に関する扱い

- 本アプリの主目的は、税額計算ではなく実現損益の把握とする
- 税務申告書の作成、提出、転記支援は行わない
- 税額表示を行う場合でも、ユーザが設定した税率を用いた参考値に留める
- 所得控除、住民税、復興特別所得税、損益通算、繰越控除等を正確に反映することは目的としない
- 給与所得からの推定限界税率表示は、MVP後の任意拡張とする

### 2.3 スコープ外（現時点では対象外）

- サーバ同期・複数端末間リアルタイム同期
- **iOS/Androidアプリのビルド・配布（将来検討）**
  - MVPはWindows専用とする
  - ただし、将来のモバイル対応可能性を保持するため、コアロジックとUI層を分離した設計を採用する（→ 5.6 参照）
- 第三者向け配布（インストーラ署名、ストア配布など）
- 税務申告書の自動生成・提出（e-Tax送信等）
- 確定申告の転記支援（税務様式に合わせた出力、提出用明細の生成等）
- Webスクレイピング/RPA等による金融機関サイトからの自動ログイン取得（原則不採用。採用可否は基本設計で判断）
- DeFi、NFT、エアドロップ、ステーキング報酬、ブリッジ等の完全な損益計算対応（将来拡張）

---

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| 基軸通貨（Base Currency） | 集計・評価の基準通貨。本アプリではJPY固定（ただしコード変更で差し替え可能にする） |
| 月次請求管理（カード） | クレジットカード利用明細を請求月・引落月単位で集計し、未引落額を負債として把握する考え方 |
| 計上日/反映日 | 明細に記録される処理日・入出金日など（取得元により名称が異なる） |
| 取引（Transaction） | 入出金・支払い・振替・売買などの出来事。アプリ上で確定された台帳上の取引を指す |
| 仕訳行（Leg） | 取引を構成する明細行（複式簿記を想定） |
| 取り込み（Import） | CSV等から明細を取得し、アプリ内データへ変換・登録する処理 |
| API取得（Fetch） | 外部サービスのAPIから明細・残高・取引履歴等を取得する処理 |
| オンチェーン取得 | ブロックチェーン上のトランザクション、残高、イベント等を取得する処理 |
| 同期（Sync） | 取得したデータを正規化し、重複・競合判定を行い、アプリ内DBへ反映する一連の処理 |
| コネクタ（Connector） | 取得元（楽天銀行、取引所、チェーン等）ごとの取得・取り込み処理モジュール |
| Rawデータ | CSV/API/オンチェーン等から取得した取得元固有の生データ |
| 正規化イベント（Normalized Event） | 取得元固有のデータを、アプリ共通の形式に変換したもの。確定取引になる前の候補データ |
| 確定取引（Confirmed Transaction） | 重複・競合確認後、台帳・残高・損益計算へ反映されるアプリ内部の取引 |
| KDF | パスフレーズから暗号鍵を導出する処理（Argon2id等） |
| スキーマバージョン | DBスキーマの版番号。Alembicで管理し、バックアップ復元時の互換性判定に使用する |
| 残高照合 | 取得元の残高情報と、台帳上の理論残高が一致するかを検証すること |
| 約定日 | 株式等で売買が成立した日付 |
| 受渡日 | 株式等で決済が完了した日付。年度判定等で利用される場合がある |
| 総平均法 | 期中の購入単価を総平均で算出し、売却損益を計算する方法（暗号資産の損益計算等） |
| 実現損益 | 売却・交換等により確定した損益。未実現損益は含まない |
| 概算税額 | ユーザ設定税率等に基づいて参考表示する税額の目安。正確性は保証しない |
| 給与所得 | ユーザが主たる勤務先から得る年間の給与収入。概算の累進税率を算出するための参考情報として利用する |

---

## 4. 前提・制約

### 4.1 前提

- 本アプリは「自分用」であり、利用者は1名を想定する
- ネットワーク接続はAPI取得・オンチェーン取得で利用する可能性があるが、基本設計はローカルDBを中心に成立すること
- UIフレームワークにはPySide6を採用する。ただし、将来のモバイル対応可能性を保持するため、コア層とUI層を明確に分離すること（→ 5.6 参照）
- 銀行・カード系はCSV取り込みを基本とし、API取得は可能な場合に限る
- 暗号資産はAPI取得・オンチェーン取得を重視するが、CSV取り込みも選択可能とする

### 4.2 制約

- アプリ種別はデスクトップアプリ（Webアプリ不可）
- 開発環境OSはWindows
- データはローカルに保存し、PC盗難・バックアップ流出を想定した暗号化が必要
- iOS向けビルドは将来検討事項であり、現フェーズでは実施しない（macOS + Xcodeが必要なため）
- 金融機関・カード会社・証券会社・取引所・ブロックチェーンAPI提供元の利用規約に反する取得方式は採用しない

---

## 5. 全体要件（非機能要件含む）

### 5.1 セキュリティ要件（最重要）

- 保存データ（DBファイル）は**常時暗号化**されていること（暗号化SQLite（SQLCipher相当）または同等方式を必須とする）
- アプリ起動時に毎回、利用者は**パスフレーズを入力**して解除すること
- パスフレーズは**長いパスフレーズ**を想定し、**コピペを許可**すること
- 初回セットアップ時にパスフレーズを**2回入力して一致確認**すること
- パスフレーズ忘れ時は**復旧不可**（データ復号不可）でよい
- バックアップファイルの作成/復元時も**パスフレーズ入力を要求**すること（ログインと同一パスフレーズ）
- 暗号鍵は端末上に平文で永続保存しないこと（パスフレーズから都度導出する）
- APIキー等の認証情報を保存する場合は、暗号化DB内またはOSの安全な資格情報ストア等で保護すること
- クリップボード貼付の注意喚起を行うこと（履歴機能等に残る可能性）

> 注: 具体ライブラリ／ドライバはPoCで「作成→再起動→パスフレーズで再オープン→CRUD→バックアップ復元」が通るものを採用する。

### 5.2 可用性・信頼性

- 同一データを複数回取り込んでも重複登録されないこと（重複排除）
- CSV/API/オンチェーンなど複数経路から同一または関連する取引が取得される可能性を考慮し、二重計上を防止できること
- 取り込み・同期・集計・可視化は、途中で失敗した場合にエラー内容を提示し、DB整合性が保たれること
- 取り込み・同期処理はトランザクション単位で処理し、失敗時に中途半端な確定取引を残さないこと
- 競合データは自動で上書きせず、確認待ち状態として保持できること

### 5.3 性能要件（暫定）

- データ件数が増えても致命的に操作不能にならないこと
  - 「多少の読み込み待ち」は許容する（快適さは最優先ではない）
- KDFによる解除時間は「標準的な設定」を採用し、極端な遅延にならない範囲で安全側に倒す
  - 初回セットアップ時または設定時にKDF実行時間を実測し、目標時間（例：0.5〜1.5秒）に収まる範囲で調整することを許容する（詳細は基本設計で確定）
- 暗号資産のオンチェーン同期はデータ量が大きくなる可能性があるため、対象チェーン・対象アドレス・取得期間を制限できる設計とする

### 5.4 保守性・拡張性

- 取得元（金融機関/カード/証券会社/暗号資産取引所/ブロックチェーン）の追加が可能な設計とする
- 資産種類（法定通貨・株式・暗号資産等）の追加が可能な設計とする
- コア機能（暗号化DB、取引モデル、台帳、集計、実現損益計算、可視化）と、取得元コネクタ（Connector）を分離する
- DBスキーマのバージョニングとマイグレーションを行えること（Alembicを使用）
- **非破壊マイグレーションポリシー**として、以下のルールを原則とする
  - カラムの削除は行わない（不要になったカラムはそのまま残す）
  - カラム追加時は必ずデフォルト値またはNULL許可とする
  - カラムのデータ型変更は行わない（必要な場合は新カラムを追加して移行する）

### 5.5 オフライン要件

- オフラインで、閲覧・集計・バックアップ/復元が可能であること
- 銀行・カード・証券のデータ取り込みはオフラインでも可能な方式（CSV等）を最低限サポートすること
- API取得・オンチェーン取得が利用できない場合でも、既に取得済みのデータを閲覧・集計できること

### 5.6 アーキテクチャ方針（UI独立性）

将来のモバイル対応可能性を保持するため、以下の層分離を設計上の必須要件とする。

```text
src/
├── core/            # PySide6依存ゼロ（ビジネスロジック・モデル・サービス）
│   ├── models/      # Transaction, Account, Asset ...
│   ├── services/    # ImportService, SyncService, LedgerService, ProfitLossService, BackupService ...
│   ├── connectors/  # RakutenBank, RakutenCard, OkinawaBank, Exchange, Chain ...
│   └── ports/       # UIが呼び出すインターフェース定義
├── infrastructure/  # DB・暗号化・ファイルI/O（UI非依存）
│   ├── db/          # SQLAlchemy models, Alembicマイグレーション
│   └── crypto/      # SQLCipher, KDF（Argon2id）
└── ui/              # PySide6はここのみに閉じ込める
    ├── windows/
    └── widgets/
```

**絶対に守るルール：`core/` および `infrastructure/` は `ui/` を import しない。**

この構造により、将来モバイル対応が必要になった場合は `ui/` 層のみを書き直すことで対応可能となり、コアロジック・DB・Connectorはそのまま再利用できる。

---

## 6. 対象データソース要件（取得・取り込み）

### 6.1 対象データソース

- 楽天銀行
- 楽天カード
- 沖縄銀行
- 証券口座（複数口座を想定）
- 暗号資産取引所（複数取引所を想定）
- 暗号資産ウォレット（複数ウォレット/アドレスを想定）
- ブロックチェーン（複数チェーンを想定）

### 6.2 取得方式の方針

#### 6.2.1 銀行・カード系

- MVPではCSV取り込みを基本とする
- 楽天カード等については、ユーザが用意したCSV取り込みを前提とする
- 公式APIまたは公認連携が利用可能で、安全かつ継続運用できる場合に限り、API取得を検討する
- APIが利用可能な取得元であっても、利用者の判断でCSV等のファイルによる取り込みを選択できること

#### 6.2.2 証券口座

- MVPではCSV取り込みを基本とする
- 将来的に証券会社API等が利用可能な場合はAPI取得を検討する
- 約定日、受渡日、数量、単価、手数料、外貨決済情報を保持できること
- 実現損益の集計に必要な取引情報を保持できること

#### 6.2.3 暗号資産

- 暗号資産については、API取得およびオンチェーンデータ取得を主要な同期方式として検討する
- 取引所・チェーン・ウォレットごとの対応差を考慮し、CSV取り込みも選択肢として残す
- 複数取引所、複数ウォレット、複数チェーンを扱える設計とする
- 売買、交換、入出庫、送金、手数料、報酬を区別できる設計とする
- 初期対応範囲は、取引所の売買・入出金・手数料、およびウォレットの送受信履歴を中心とする
- DeFi、NFT、エアドロップ、ステーキング報酬、ブリッジ等は将来拡張とする

### 6.3 取得方式の未確定事項（要決定）

| 優先度 | 方式 | 概要 | MVPでの扱い |
|---:|---|---|---|
| 1 | CSV（明細ダウンロード取り込み） | ユーザが取得元サイト等から明細をダウンロードし、アプリが取り込む | 銀行・カード・証券で必須 |
| 2 | API（公式APIまたは同等の公認連携） | 認証・認可を経て明細を自動取得 | 銀行・カード・証券では可能なら採用 |
| 3 | 暗号資産API | 取引所API等から取引履歴・残高を取得 | 暗号資産では主要方式として検討 |
| 4 | オンチェーン取得 | tx_hash、ログ、残高、ウォレット履歴等を取得 | 暗号資産では主要方式として検討 |
| 5 | Webスクレイピング/RPA等 | 画面操作自動化による取得 | 原則不採用（規約・2FA・安定性の観点） |

> 本要件定義書では、銀行・カード・証券についてはMVPとして少なくとも「CSV取り込み」を必須要件として保証する。  
> 暗号資産についてはAPI取得・オンチェーン取得を重視するが、CSV取り込みも継続して選択可能とする。

### 6.4 取り込み・同期の共通要件

取得元ごとにConnectorを持ち、以下を実現する。

- 取得元データ（CSV/API/オンチェーン等）を取り込めること
- Rawデータとして取得元固有のデータを保存できること
- Rawデータを正規化イベントへ変換できること
- 正規化イベントを確定取引へ反映する前に、既存データとの重複・関連・競合チェックを行えること
- 重複排除（source + external_id、tx_hash、ハッシュ等）を行い、二重登録を防止すること
- 取り込み結果（件数、スキップ件数、エラー、確認待ち件数、競合件数）をログ表示すること
- 取得元データ取得手段（API/CSV/オンチェーン）に依存せず、同一の正規化イベントへ変換し、コア処理を共通化すること
- 銀行口座の将来的な残高照合に備え、取得元データに残高情報が存在する場合は、明細行の「取引後残高（`balance_after`）」を保存できること
- 取り込み時にCSV内の順序を保持できる情報（例：行番号 `row_index`、ファイル識別子等）を保存できること

### 6.5 日付の取り扱い

日付は概念を分けて保持すること。

- `occurred_at`（発生日。取引が実際に発生した日付）
- `posted_at`（計上日/反映日。口座残高に反映された日付）
- クレジットカード利用については、利用日を明細属性として保持しつつ、負債額の管理は請求月・引落月単位で行えること（→ 7.4）
- 株式等については `trade_date`（約定日）および `settlement_date`（受渡日）を保持できること（→ 10.6）
- 取得元の日時は、取得元タイムゾーンと正規化後の日時を分けて保持できること
- 内部処理ではUTCまたは明示的な基準タイムゾーンに正規化し、表示・集計時に用途に応じた日付へ変換できること

---

## 7. 台帳・競合・整合性要件

### 7.1 台帳の基本方針

台帳は、取得元データをそのまま確定取引として扱うのではなく、**Rawデータ、正規化イベント、確定取引**を分離して管理する。

```text
Raw Data
  ↓
Normalized Event
  ↓
Candidate Match / Conflict Check
  ↓
Confirmed Transaction
```

- 取得元データは、まずRawデータとして保存する
- Rawデータを正規化イベントへ変換する
- 正規化イベントを既存取引と照合し、重複・関連取引・競合の有無を判定する
- 問題がない場合のみ、確定取引として登録する
- 競合がある場合は、ユーザ確認待ち状態として保持する

### 7.2 取得元由来データとユーザ編集データの分離

- 取得元由来の事実データは原則として直接編集しない
- ユーザによるカテゴリ、メモ、表示名、取引種別の補正、損益計算上の扱いは別データとして保持する
- CSV再取り込み、API再同期、オンチェーン再取得により取得元データが更新されても、ユーザ編集データを自動上書きしないこと

### 7.3 データ競合・重複管理

CSV取り込み、API取得、オンチェーン取得など、複数の経路から同一または関連する取引データが取得される可能性がある。そのため、取り込み時には重複登録・二重計上・矛盾データの発生を防ぐ設計とする。

- 完全一致するデータは自動的に重複としてスキップできること
- 同一取引の可能性が高いデータは、既存取引への紐づけ候補として扱うこと
- 自動判定できない競合は、ユーザ確認待ち状態として保持すること
- 競合データは自動で上書きしないこと
- 1つの確定取引に対して、複数の取得元イベントを紐づけられる設計とする
- 競合判定に用いた根拠（日時、金額、通貨、外部ID、tx_hash 等）を確認できること

### 7.4 想定する競合パターン

| 種類 | 例 | 対応方針 |
|---|---|---|
| 完全重複 | 同じCSVを再取り込み | 自動スキップ |
| 同一取引の別経路取得 | 取引所APIとCSVに同じ約定が存在 | 同一取引として統合候補にする |
| オンチェーンと取引所履歴の対応 | 出金履歴とtx_hashが一致/近似 | 資産移動として紐づける |
| 金額差異 | 手数料控除後の金額だけ違う | 差分を表示してユーザ確認 |
| 日時差異 | APIとCSVでタイムゾーンや反映時刻が違う | 許容範囲内なら同一候補 |
| 一部欠損 | CSVにtx_hashがない | 金額・通貨・日時・アドレス等で推定 |
| 矛盾 | 同じ外部IDだが金額が違う | 自動上書きせず競合として提示 |

### 7.5 取引識別子

- アプリ内部の取引IDは、取得元に依存しない `transaction_id` とする
- 取得元ごとの識別子は `source_event_id` として別管理する
- 同一の確定取引に、複数の `source_event_id` を紐づけられること
- 1つの取得元イベントから複数の確定取引を生成できる設計を検討すること

### 7.6 台帳の状態管理

取り込んだデータがすぐに確定取引になるとは限らないため、以下の状態を扱える設計とする。

- `imported`：取り込み済み
- `normalized`：正規化済み
- `pending_review`：確認待ち
- `confirmed`：確定
- `ignored`：無視
- `conflicted`：競合あり
- `reconciled`：照合済み

### 7.7 口座間移動・資産移動の扱い

- 口座間移動、証券口座への入出金、取引所と自己ウォレット間の送金、ウォレット間送金は、原則として収入・支出・実現損益として扱わない
- 資産移動は `transfer` として分類し、売却・購入・交換・手数料・報酬と区別できること
- 取引所からの出金とオンチェーン上の入金が同一資産移動と推定される場合、資産移動として紐づけ、二重に損益計算へ反映しないこと

### 7.8 手数料の扱い

- 手数料は通常の支出とは区別して保持する
- 手数料が取得価額、売却費用、送金費用、ガス代のいずれに該当するかを分類できる設計とする
- 暗号資産では、スワップ、ガス代、送金手数料、入出庫手数料を区別して保持できること

### 7.9 残高差異・調整取引

- 台帳上の理論残高と、取得元から取得した実残高を区別して保持・比較できること
- 差異がある場合は、差額・対象口座・対象期間を確認できること
- 残高差異を補正するための調整取引を登録できること
- 調整取引は通常取引と区別できること

### 7.10 削除・除外

- 取引は原則として物理削除しない
- 削除操作は `is_deleted` または `status=ignored` として扱う
- 損益計算・残高計算から除外できること
- Rawデータや取り込み履歴は保持し、後から確認・再判定できるようにする

### 7.11 精度・丸め

- 金額・数量・レートは浮動小数点ではなく、Decimal等の固定精度で扱う
- 資産ごとに数量精度を保持できること
- 暗号資産の小数桁、株式数量、為替レート、手数料を安全に扱える精度を確保する

### 7.12 操作履歴

- 台帳データの作成・更新・除外・統合・分割について、操作履歴を保持できる設計とする
- 実現損益の根拠となる取引、取得元イベント、ユーザ補正内容を後から確認できること

---

## 8. 機能要件

### 8.1 初回セットアップ

- DB未作成の場合、初回セットアップ画面を表示する
- パスフレーズを2回入力し一致した場合のみ、暗号化DBを作成する
- パスフレーズ忘れは復旧不可である旨を明示する

### 8.2 起動・ロック解除

- アプリ起動時はロック画面を表示し、パスフレーズ入力で解除する
- アプリロック解除後は、DBにアクセス可能となる
- パスフレーズ入力欄はマスクし、表示/非表示を切り替え可能とする
- コピペを許可する

### 8.3 資産・負債の管理（基本）

- 法定通貨（JPY）を管理できること
- 株式・暗号資産を管理できること
- 将来拡張として、新しい資産種類を追加できること

### 8.4 クレジットカード負債管理（月次請求単位）

- クレジットカード利用明細は、原則としてユーザが用意したCSV等を取り込んで登録する
- MVPでは、クレジットカード会社のAPIによるリアルタイム取得は前提としない
- カード利用日は明細情報として保持する
- 負債額の管理は、利用日単位ではなく、請求月・引落月単位を基本とする
- 請求月ごとの利用額を集計し、未引落分をクレジットカード負債として表示する
- 引落予定日、引落口座、引落済み/未引落の状態を管理できる設計とする
- 銀行口座明細との自動照合はMVP後の拡張とする

### 8.5 口座／ウォレット単位の管理（情報設計方針）

- 本アプリは、口座（銀行口座／証券口座／クレジットカード等）およびウォレット（暗号資産取引所口座／オンチェーンウォレット等）単位で管理する
- 口座／ウォレットはそれぞれ独立して閲覧できる
- ホーム画面では全口座／全ウォレットを集約した一元ビューを提供する
  - 概要（簡易な財務情報）と詳細（財務諸表相当の詳細情報）を切り替えられること

> 注: 具体的な画面UIは基本設計で確定する。ここでは情報設計（口座粒度／集約粒度）の方針を要件として固定する。

### 8.6 取り込み・同期

- **手動取り込み**
  - UI操作でCSV等の取り込みを開始できる
- **API同期**
  - 対応取得元について、APIから取引履歴・明細・残高を取得できる設計とする
- **オンチェーン同期**
  - 対応チェーンについて、ウォレットアドレス単位でオンチェーンデータを取得できる設計とする
- **定期同期**
  - アプリ起動中の定期実行をサポートする（間隔は設定可能を想定）
  - 将来、アプリ非起動時の同期（CLI + タスクスケジューラ等）も検討する
- **同期の動作モード**
  - 取得元がAPI取得とCSV取り込みの両方に対応する場合、ユーザはどちらを利用するか選択できること
  - API取得が利用できない取得元、またはユーザがCSV取り込みを選択した取得元については、「ユーザが配置/指定したCSV等の取り込み」を行う
  - 取得元ごとに、利用可能な動作モード（API/CSV/オンチェーン）をUI上で明示する

### 8.7 可視化・レポート

- 資産・負債バランスの可視化（BS）
- 収支の可視化（PL相当、期間集計）
- 収支・残高推移のグラフ表示
- クレジットカード請求月・引落月ごとの未引落額表示
- **実現損益サマリ（年次/任意期間）**
  - 株式（日本株・米国株）および暗号資産の実現損益を表示できること
  - 口座／ウォレット単位および全体集約で表示できること
  - 税額計算ではなく、損益把握を主目的とすること
- **税額参考表示（任意拡張）**
  - ユーザが設定した税率をもとに参考値として表示できること
  - 正確な税額算出ではない旨をUI上に明示すること

### 8.8 バックアップ/復元（端末移行）

#### バックアップ作成

- パスフレーズ入力を要求し、正しいことを確認した上でバックアップを出力する
- 出力先はUSB/外付け/クラウドストレージ等、任意のパスを選択可能とする
- バックアップファイルには**スキーマバージョンをメタ情報として必ず記録**する

```text
backup_20260601/
├── data.db       # 暗号化SQLite本体
└── meta.json     # {"schema_version": 3, "app_version": "1.0.0", "created_at": "..."}
```

#### 復元

- バックアップファイルを選択し、パスフレーズ入力で復元可能とする
- 復元時は既存DBを退避する等、破壊的操作の確認を行う（詳細は基本設計）
- **復元時のスキーマバージョン互換性チェックを行う**

| バックアップ vs 現アプリ | 動作 |
|---|---|
| バックアップが古い（スキーマ < 現在） | Alembicマイグレーションを自動実行して復元 |
| 同じバージョン | そのまま復元 |
| バックアップが新しい（スキーマ > 現在） | 「アプリを更新してください」とエラー表示し、復元しない |

### 8.9 設定（Preferences）

- アプリの設定画面を持つこと
- 取得元ごとに、利用する取得方式（CSV/API/オンチェーン）を選択できること
- APIキー等の認証情報を保存・削除できる設計とする
- 設定画面から、税額参考表示に用いる税率をユーザが変更できること
  - 株式向け税率（参考値）
  - 暗号資産向け税率（参考値）
- 暗号資産の税額参考表示のため、ユーザが給与所得（年収）を入力できる設計とする
  - 入力された給与所得に基づき、所得税の累進課税を考慮した推定限界税率の目安を表示する機能は任意拡張とする
  - 表示された目安税率は参考情報であり、ユーザはこれを参考に税率を任意に上書きできること

---

## 9. 画面要件（暫定）

### 9.1 必須画面（MVP）

- 初回セットアップ（DB作成、パスフレーズ2回入力）
- ロック解除（起動時パスフレーズ入力）
- メイン画面（ダッシュボード）
- 取引/明細一覧（台帳）
- 取り込み（同期）画面／同期ログ表示
- 競合・確認待ち取引の確認画面
- クレジットカード請求一覧画面
- 実現損益画面（年次/任意期間）
- バックアップ作成／復元
- 設定画面（取得方式、税率参考値、給与所得入力等）

### 9.2 画面方針

- 「簡単な家計簿的UI」と「会計知識が必要なUI（財務諸表・勘定系）」の両方を実装する
- 口座／ウォレット単位のページ（ビュー）を持つ
- ホーム画面で一元集計し、概要↔詳細を切り替え可能にする
- 実現損益については、根拠となる取引・取得元イベントを確認できること
- 税額参考表示については「目安」であることを明示する
- 詳細なUIワイヤーフレームは基本設計で作成する

---

## 10. データ要件（暫定）

### 10.1 基軸通貨

- 基軸通貨は JPY とする
- 将来、ソースコード変更で差し替え可能な構造とする（設定ファイル/定数の一元化）

### 10.2 取引モデル（方向性）

- 取引（Transaction）と仕訳行（Leg）を中心に設計し、資産タイプ追加に耐えること
- 取得元のRawデータも保存し、再現性・監査性を確保すること
- 取得元由来のデータと、ユーザ編集データを分離できること
- 表示用カテゴリと、損益計算用の取引種別を分けて保持できること

### 10.3 取り込み元・取得元イベント

- 取り込み元ファイル、API取得、オンチェーン取得の単位を識別できること
- 取得元名、取得方式、取り込み日時、ファイル名、ファイルハッシュ、APIエンドポイント、チェーンID、ウォレットアドレス等を保持できること
- SourceEventとして、取得元イベントを確定取引と別に保持できること
- 1つの確定取引に複数のSourceEventを紐づけられること

### 10.4 明細の残高情報（残高照合の前提）

- 銀行明細について、取得元に残高列が存在する場合は、明細行ごとに `balance_after`（取引後残高）を保持できること
- 残高照合の実装に備え、以下の情報を保持できること（取得元に存在しない場合はNULL可）
  - `posted_at`
  - `balance_after`
  - `external_id`（取得元側のユニークID。存在しない場合はハッシュで代替）
  - `source_file_id`（どのファイル/API取得から来たかの識別子）
  - `row_index`（ファイル内の行番号）

### 10.5 クレジットカード明細・請求データ

- `card_used_at`：カード利用日
- `billing_month`：請求月
- `payment_month`：引落月
- `payment_due_date`：引落予定日
- `withdrawal_account_id`：引落口座
- `statement_status`：未確定 / 確定 / 引落済み
- `card_account_id`：対象カード
- `merchant_name`：利用先
- `amount`：利用金額

### 10.6 株式取引の日付要件（約定日・受渡日）

- 株式（日本株・米国株）について、取引データとして少なくとも以下の日付を保持できること
  - `trade_date`（約定日）
  - `settlement_date`（受渡日）
- 年度判定等で受渡日を利用できるようにすること（例：実現損益サマリの対象年の判定）

### 10.7 為替換算ルール（米国株等）

- 外貨建て資産（米国株等）の損益・評価額のJPY換算について、為替換算ルールを選択可能とする
  - 例：**約定日レート**／**受渡日レート**
- デフォルトは「**約定日レート**」とする
- 年度判定は受渡日を保持し、必要に応じて利用できるようにする（例：実現損益サマリの集計対象年の判定）
- 取引通貨、取引通貨建て金額、基軸通貨換算額、換算レート、レート取得元、レート基準日を保持できる設計とする

### 10.8 暗号資産データ

- オンチェーンデータでは、`chain_id`、`tx_hash`、`log_index`、`wallet_address` を保持できること
- 取引所データでは、`exchange_name`、`external_id`、`order_id`、`trade_id`、`withdrawal_id`、`deposit_id` 等を保持できる設計とする
- 暗号資産の数量、手数料、ガス代、対価通貨、交換先資産を保持できること
- 取引所からの出金とオンチェーン上の入金が同一資産移動と推定される場合、二重に損益計算へ反映しないこと

### 10.9 実現損益と税額参考表示の前提

- 本アプリの主目的は、税額計算ではなく実現損益の把握とする
- 株式と暗号資産の実現損益を年次または任意期間で集計できること
- 税額を表示する場合は、ユーザが設定した税率に基づく参考値とする
- 所得控除、住民税、復興特別所得税、損益通算、繰越控除等は正確な計算対象としない
- 税額参考表示は正確性を保証しない（目安）ことをUI上で明示する
- 税率（参考値）はユーザ設定可能とし、以下を保持できること
  - `estimated_tax_rate_stock`（株式向け参考税率）
  - `estimated_tax_rate_crypto`（暗号資産向け参考税率）
- ユーザの給与所得（年収）を保持できる設計とする
  - `salary_income`（給与所得）

### 10.10 スキーマ互換性方針

- スキーマ変更は非破壊マイグレーションポリシー（→ 5.4）に従う
- Alembicによりスキーマバージョンを管理し、upgrade/downgradeスクリプトを両方実装する
- downgradeスクリプトは、データ損失が生じる場合はその旨をコメントで明記する

---

## 11. 運用要件

- 自分用運用のため、ユーザー管理（複数アカウント）は不要
- バックアップは利用者が適宜実施する（USB/外付け/クラウド）
- クラウド保管を想定し、バックアップファイル流出時も復号困難であることを前提に安全側のKDF設定とする
- APIキー、ウォレットアドレス、同期設定は、取得元ごとに管理できること
- API仕様、CSV形式、チェーン仕様は変更される可能性があるため、Connector単位で差し替え可能な構造とする

---

## 12. 未決事項・今後の検討項目

| 項目 | 状態 | 決定タイミング |
|---|---|---|
| UIフレームワーク | ✅ 確定（PySide6） | 済 |
| 対象プラットフォーム（MVP） | ✅ 確定（Windowsデスクトップ専用） | 済 |
| iOS/Android対応 | 将来検討（コア/UI分離設計で可能性を保持） | MVP後 |
| 楽天銀行／楽天カード／沖縄銀行の取得方式 | CSV基本。APIは可能なら検討 | Connector設計時 |
| 各取得元のデータ形式（CSVヘッダ、日付、金額符号、ユニークID有無） | 未確定 | Connector実装前 |
| 暗号資産取引所APIの対象 | 未確定 | 暗号資産同期設計時 |
| 対応チェーン・ウォレット種別 | 未確定 | オンチェーン同期設計時 |
| 同期の定期実行方式（アプリ起動中のみか、タスクスケジューラ連携までMVPに含めるか） | 未確定 | 基本設計 |
| 具体的な暗号化方式（暗号化SQLiteの採用可否、ライブラリ、配布形態） | 未確定（PoC検証後に確定） | PoC後 |
| KDFパラメータの決定（実測調整の有無） | 未確定 | PoC後 |
| 会計UI（BS/PL/試算表）と簡易UIの具体仕様 | 未確定 | 基本設計 |
| カード引落シミュレーションの詳細仕様 | 未確定 | 基本設計 |
| 自動分類ルール（摘要/店名からのカテゴリ推定等）の採用可否 | 未確定 | MVP後 |
| 競合解決UIの具体仕様 | 未確定 | 基本設計 |
| 残高照合（銀行明細の積み上げと残高の一致検証） | 将来拡張（データ保持は要件で必須） | MVP後 |
| 概算税額に用いる税率のデフォルト値（株式／暗号資産） | 未確定 | 損益・税額参考表示設計時 |
| 株式の対象範囲（ETF/投信/ADR等を含むか） | 未確定 | 投資機能設計時 |
| 配当・分配金を実現損益・税額参考表示に含めるか | 未確定 | 投資機能設計時 |
| 為替レートの取得元・保持方式（API/CSV/手入力、レートテーブル設計） | 未確定 | 投資機能設計時 |
| DeFi、NFT、ステーキング報酬等の扱い | 将来拡張 | MVP後 |

---

## 13. 受入条件（暫定）

MVPの受入条件として、最低限以下を満たすこと。

1. 初回セットアップでパスフレーズを設定し、暗号化DBが作成できる
2. アプリ起動時に毎回パスフレーズ入力が必要で、正しい場合のみデータにアクセスできる
3. 暗号化DBファイルを外部にコピーしても、パスフレーズなしでは内容が読み取れない
4. 取り込みにより明細を登録できること（API自動取得が利用可能な場合でも、ユーザの選択によりAPIまたはCSV取り込みのいずれでも登録できること）
5. 同じ明細を再取り込みしても重複登録されない
6. CSV/API/オンチェーン等の複数経路から同一または関連する取引が入った場合に、重複・競合・確認待ちとして扱える設計であること
7. クレジットカード利用明細をCSV等で取り込み、請求月・引落月単位で未引落額をクレジットカード負債として確認できること
8. 株式（日本株・米国株）の取引で `trade_date`（約定日）と `settlement_date`（受渡日）を保持できること
9. 外貨建て資産のJPY換算ルールを「約定日レート／受渡日レート」等から選択でき、デフォルトが約定日レートであること
10. 株式および暗号資産の実現損益を年次または任意期間で集計し表示できること
11. 暗号資産について、API取得・オンチェーン取得・CSV取り込みのいずれか、または複数方式を想定したConnector分離設計があること
12. 取引所出金とオンチェーン入金等を二重に損益計算へ反映しない設計であること
13. BS/収支等の基本可視化ができる（簡易でよい）
14. バックアップ作成・復元ができ、USB/外付け/クラウド任意パスに保存できる
15. 復元時にスキーマバージョンを確認し、古いバックアップは自動マイグレーション、新しいバックアップはエラーとなること
16. 取得元追加・資産種類追加を想定した構造（Connector分離、取引中心モデル、スキーマバージョン管理）がある
17. `core/` および `infrastructure/` が `ui/` に依存していないこと（UI独立性の検証）
18. 銀行明細取り込みで残高列が存在する場合、`balance_after` と `posted_at` を保持できること（存在しない場合はNULLでも動作すること）
19. 明細は `occurred_at` と `posted_at` を概念として分けて保持し、用途（家計簿的集計／残高推移）に応じて利用できること
20. 税額参考表示を行う場合は、正確な税額計算ではなく参考値であることをUI上に明示できること
21. 設定画面で税率（株式向け／暗号資産向け）を変更でき、税額参考表示に反映されること
22. 設定画面で給与所得を入力し、参考となる推定限界税率の目安を確認できる設計であること

---

## 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-06-01 | 初版作成 |
| 2026-06-01 | UIフレームワークをPySide6に確定。5.6（アーキテクチャ方針）を追加。7.7にスキーマバージョン互換性要件を追加。5.4に非破壊マイグレーションポリシーを追加。9.3（スキーマ互換性方針）を追加。11（未決事項）を状態付きテーブルに更新 |
| 2026-06-01 | 取得方針を「API優先・不可ならCSV等ダウンロード取り込み」に更新（6.2/6.3/7.6/11/12を更新）。5.1で暗号化DB方式を必須化し、5.3にKDF実測調整の方針を追記。 |
| 2026-06-01 | 将来の残高照合に備え、`balance_after` / `posted_at` 等の保持要件を追加（6.4、9.4、9.5、11、12を更新）。 |
| 2026-06-01 | 口座/ウォレット単位管理方針、株式の約定日/受渡日保持、為替換算ルール選択（デフォルト約定日）を追加（6、7、8、9、11、12を更新）。 |
| 2026-06-01 | 税務要件を「確定申告支援」から「実現損益＋概算税額（株式/暗号資産で税率分離）」へ縮小（1、2、3、7.7、9.8、11、12を更新）。 |
| 2026-06-01 | 概算税額の税率をユーザが設定画面で変更できる要件を追加（2.1、7.9、8.1、9.8、11、12を更新）。 |
| 2026-06-02 | 銀行APIの取得方式について、API利用時でもユーザがCSV取り込みを選択できる「選択制」であることを明記（6.2、6.3、7.6、11、12を更新）。 |
| 2026-06-02 | 暗号資産の税率設定に関連し、給与所得入力機能とそれに基づく推定限界税率表示の要件を追加。用語定義に「給与所得」を追加（3、7.9、9.8、11、12を更新）。 |
| 2026-06-04 | クレジットカード負債管理について、発生主義（利用日基準）から、CSV取り込みを前提とした請求月・引落月単位の月次請求管理へ方針を変更（1、3、6.4、7.4、12を更新）。 |
| 2026-06-04 | 株式・暗号資産の実現損益を主目的とし、税額計算は参考表示へ整理。銀行・カード系はCSV基本、暗号資産はAPI/オンチェーン同期を重視する方針へ変更。CSV/API/オンチェーンの複数経路による重複・競合・二重計上リスクに対応するため、台帳・競合・整合性要件を追加。 |
