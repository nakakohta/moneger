# 要件定義書（moneger MVP）

| 項目 | 内容 |
|---|---|
| 文書名 | 要件定義書 |
| プロジェクト名 | moneger |
| 作成日 | 2026-06-01 |
| 最終更新日 | 2026-08-13 |
| 対象ユーザー | 作成者本人（自分用） |
| 対象OS | Windows |
| 開発言語 | Python |
| UIフレームワーク | PySide6 |
| 本文書の対象 | MVP |

---

## 1. 背景・目的

moneger は、銀行、クレジットカード、証券口座、暗号資産取引所、ブロックチェーン等に分散している個人金融データを、CSV・API・オンチェーン等から収集し、**取得元ごとに定義された決定論的な変換ルール**によって共通形式へ整理し、ローカル環境で一元管理するためのWindowsデスクトップアプリである。

収集・整理したデータを利用し、以下を把握できることを目的とする。

- 現金・銀行口座等の残高
- クレジットカードの請求・未引落額
- 証券口座の保有資産・評価額
- 暗号資産の残高・移動履歴
- 資産・負債・収支の概要
- 株式および暗号資産の実現損益
- データが古い、不足している、同期に失敗している等のデータ状態

本アプリの中心は、**金融データの収集・正規化・統合・状態管理**である。BS/PL表示、実現損益等は、その統合データを利用する派生機能として扱う。

> 注: 本アプリは税務申告書の作成・提出を目的としない。実現損益は取得データと設定条件に基づく参考情報であり、税務上の正確性・完全性を保証しない。

---

## 2. MVPの基本方針

### 2.1 MVPで優先するもの

MVPでは、以下の順に価値を成立させる。

1. 金融機関・サービスごとのデータを安全に取得・取り込める
2. 取得元固有のデータを共通形式へ変換できる
3. 同じデータを再取り込みしても壊れない
4. データの重複・競合・不足・古さを把握できる
5. 口座・資産を横断して現在の状態を確認できる
6. 必要なデータが揃っている場合に実現損益を確認できる

### 2.2 正規化方針

- MVPではAI・LLMによる正規化、分類、推測は行わない
- 正規化は、Connector / Parserに定義された固定ルールによる**決定論的変換**とする
- 同一の入力データと同一バージョンのParserからは、原則として同一の正規化結果が得られること
- 想定外のCSV形式を推測して取り込まない
- フォーマット不一致時は、未対応形式またはエラーとしてユーザに通知する

### 2.3 MVP後へ送るもの

以下はMVP後の拡張とする。

- 税額参考表示
- 給与所得入力・推定限界税率表示
- 税務申告支援
- アプリ非起動時の定期同期
- 高度な自動競合解決
- 高度な残高照合・自動補正
- DeFi、NFT、エアドロップ、ステーキング、ブリッジの完全解析
- iOS / Android対応
- 複数端末同期・サーバ同期
- AIによる分類・分析・正規化

---

## 3. MVPスコープ

### 3.1 対象プラットフォーム

- Windowsデスクトップアプリ
- ローカルDBを中心とする
- ネットワーク未接続でも、取得済みデータの閲覧・集計・CSV取り込み・バックアップ/復元が可能であること

### 3.2 初期対象データソース

#### 銀行・カード

- 楽天銀行
- 楽天カード
- 沖縄銀行

MVPではCSV取り込みを基本とする。銀行・カード系APIはMVP必須としない。

#### 証券

- **楽天証券を初期対応証券会社とする**
- MVPでは楽天証券のCSV取り込みを基本とする
- 対応CSVは実際のエクスポートデータを確認しながら順次確定する
- 保有残高CSVと取引履歴CSV等は、用途の異なる別データ種別として扱う

#### 暗号資産

初期対応対象は以下とする。

- Bitcoin / BTC
- Ethereum / ETH
- Solana / SOL

暗号資産については、以下の取得方式を想定する。

- オンチェーン取得
- 暗号資産取引所API（読み取り専用。対象取引所は別途決定）
- CSV取り込み

MVPでは、オンチェーン取得について少なくとも残高・送受信履歴の取得を中心とする。取得価額・実現損益の算出に必要な情報がオンチェーン情報だけで不足する場合は、取引所CSV/API等との組み合わせを前提とする。

### 3.3 MVP対象外

- Webスクレイピング/RPAによる金融機関サイトへの自動ログイン
- 秘密鍵・シードフレーズを利用したウォレット操作
- 注文・売買・出金等の金融取引実行
- 第三者向け配布機能
- 税務申告書の自動生成・提出

---

## 4. 用語定義

| 用語 | 定義 |
|---|---|
| Provider | 楽天銀行、楽天証券等のデータ提供元 |
| Data Source / Data Type | Provider内のデータ種別。例: 保有残高CSV、取引履歴CSV |
| Connector | 外部サービスとの取得処理またはProvider単位の統合窓口 |
| Parser | CSV等の特定フォーマットを解析し、Rawデータを正規化する処理 |
| Rawデータ | CSV/API/オンチェーン等から取得した元データ。再解析可能性を確保するため保持する |
| Normalized Event | 取得元固有のデータをmoneger共通形式へ決定論的に変換したイベントデータ |
| Snapshot | 特定時点の残高・保有数量・評価額・為替レート等の状態データ |
| Transaction | 入出金、支払い、売買、送金等の確定取引 |
| Leg | Transactionを構成する明細行 |
| Source Event | 取得元に存在する1件のイベント。Transactionとは別に識別する |
| Import | CSV等のファイルからデータを取り込む処理 |
| Fetch | APIまたはオンチェーンから外部データを取得する処理 |
| Sync | Fetchしたデータを正規化・重複判定し、DBへ反映する一連の処理 |
| 実現損益 | 売却・交換等により確定した損益。未実現損益は含まない |
| データ鮮度 | 最終取得・取り込み日時等から判断するデータの新しさ |
| データ完全性 | 必要なデータ種別・期間が揃っているかという状態 |
| KDF | パスフレーズから暗号鍵を導出する処理 |

---

## 5. セキュリティ・非機能要件

### 5.1 暗号化

- 保存DBは常時暗号化する
- SQLCipher相当または同等の暗号化SQLite方式を採用する
- アプリ起動時に毎回パスフレーズ入力を要求する
- 初回セットアップ時はパスフレーズを2回入力し一致確認する
- パスフレーズ忘れ時は復旧不可でよい
- 暗号鍵を平文で永続保存しない
- KDFはArgon2id等の現実的な方式をPoCで検証して採用する

### 5.2 API・ウォレット安全性

- 暗号資産取引所APIは**読み取り専用権限**を原則とする
- 注文・取引・出金権限を持つAPIキーの利用はMVP対象外とする
- APIキー等の認証情報は暗号化DBまたはOSの安全な資格情報ストアで保護する
- ウォレットの秘密鍵、シードフレーズ、署名権限は扱わない
- オンチェーン取得は公開アドレスまたは読み取り専用APIを前提とする

### 5.3 ログ・一時データ

- ログにパスフレーズ、APIキー、秘密鍵、シードフレーズ等を出力しない
- 口座番号、カード番号等の機微情報も必要以上にログへ出力しない
- CSV等の一時ファイルを作成する場合、不要になった一時ファイルを残さない
- エラー表示は原因把握に必要な情報に限定する

### 5.4 信頼性

- 同一データを再取り込みしても重複登録されないこと
- 取り込み・同期失敗時に中途半端な確定データを残さないこと
- DB書き込み処理は適切なトランザクション境界を持つこと
- 競合時は安全側に倒し、不確実な自動統合を行わないこと

### 5.5 数値精度

- 金額、数量、為替レートは `float` を前提とせず、Decimal等の固定精度で扱う
- BTC、ETH、SOL等、資産ごとに必要な数量精度を保持できること

---

## 6. アーキテクチャ要件

### 6.1 UIとコアの分離

```text
src/
├── core/                  # UI非依存
│   ├── models/
│   ├── services/
│   └── ports/
├── connectors/            # Provider / DataSourceごとの取得・解析
│   ├── banking/
│   ├── cards/
│   ├── securities/
│   └── crypto/
├── infrastructure/
│   ├── db/
│   ├── crypto/
│   └── files/
└── ui/                    # PySide6
```

- `core/` と `infrastructure/` は `ui/` を import しない
- UI変更がコアロジック・Connectorへ波及しにくい構造とする

### 6.2 Connector / Parserの粒度

Connector / Parserは、単なる「金融機関単位」だけではなく、**Provider × Data Type × Format Version** を識別できる構造とする。

例:

```text
RakutenSecurities
├── HoldingsCsvParser
├── DomesticTradeCsvParser
├── UsTradeCsvParser
└── RealizedProfitLossCsvParser
```

- 同一Providerでも異なるCSV種別を別Parserとして扱えること
- CSV仕様変更時に新旧Parserを共存または切り替え可能な設計とする
- 想定フォーマットと一致しない場合は取り込みを停止する

### 6.3 データ処理フロー

```text
External Data
    ↓
Raw Data / Raw Snapshot
    ↓
Deterministic Parser
    ↓
Normalized Event / Snapshot
    ↓
Duplicate / Conflict Check
    ↓
Confirmed Transaction / Stored Snapshot
```

- Event系データとSnapshot系データを分けて扱えること
- すべての外部データをTransactionへ無理に変換しないこと

---

## 7. データモデル要件

### 7.1 Event / Transaction系

以下のようなデータを対象とする。

- 銀行入出金
- カード利用明細
- 株式売買
- 暗号資産売買
- 暗号資産送受信
- 手数料
- 資産移動

TransactionとSource Eventを別に識別する。

### 7.2 Snapshot系

以下のような特定時点の状態データを扱えること。

- 銀行残高
- 楽天証券の保有銘柄・保有数量・評価額
- BTC / ETH / SOLの残高
- 為替レート
- その他、取得元が提供する残高・評価データ

Snapshotには少なくとも以下を持てること。

- 対象口座 / ウォレット
- 対象資産
- 数量または金額
- 評価日時
- 取得日時
- 取得元
- Source ID / Import ID

### 7.3 Rawデータ保持

- 元CSV/APIレスポンス/オンチェーン取得情報を、後から取得元と対応づけられる形で保持する
- Parserの不具合修正や仕様変更時に再正規化できることを考慮する
- 元ファイルそのものをDBへ保存するか、Raw行/JSONとして保存するかは基本設計で決定する
- ファイルハッシュ、ファイル名、取り込み日時、Parserバージョン等を保持できること

### 7.4 日付・時刻

- `occurred_at`：発生日
- `posted_at`：反映日
- 株式では `trade_date` と `settlement_date` を保持できること
- 取得元タイムゾーンを保持または解釈できること
- 内部時刻はUTC等の明示的な基準へ正規化する

---

## 8. 重複・競合・整合性要件

### 8.1 基本方針

- CSV/API/オンチェーン等の複数経路から同一データが入る可能性を前提とする
- 重複判定と競合判定を分ける
- 不確実な一致は自動統合しない

### 8.2 自動重複判定

次のような確度の高い識別子が一致する場合、自動重複判定に利用できる。

- external_id
- trade_id
- order_id
- tx_hash
- chain_id + tx_hash + log_index
- 同一ファイルハッシュ + row_index

### 8.3 統合候補

以下のような近似一致は、自動統合ではなく確認候補として扱う。

- 金額・通貨が一致し日時が近い
- 出金と入金の数量が手数料差を除き近い
- CSVにはtx_hashがないが、時刻・数量・アドレスが近い

### 8.4 資産移動

- 自分の銀行口座間移動
- 証券口座への入出金
- 取引所から自己ウォレットへの送金
- 自己ウォレット間の送金

これらは原則として収入・支出・実現損益として二重計上しない。

### 8.5 除外・修正

- 取得元由来の事実データを直接上書きしない
- ユーザ補正は別データとして保持する
- 取引は原則として物理削除せず、除外状態を持てること
- ユーザ補正が再取り込みで消えないこと

---

## 9. 取得・取り込み要件

### 9.1 CSV取り込み

- UIからCSVファイルを選択して取り込めること
- Provider / Data Type / Parserを識別できること
- 同一CSVを再取り込みしても重複しないこと
- 未対応形式は推測して取り込まず、理由を表示すること
- 取り込み結果として、登録・重複・確認待ち・エラー件数を表示すること

### 9.2 楽天証券

MVPでは楽天証券を初期証券会社として扱う。

- 保有残高CSVをSnapshotとして取り込める構造とする
- 保有残高CSVは現在保有資産・評価額等の確認に利用する
- 実現損益の算出には、別途取引履歴等の必要データが揃っていることを前提とする
- 対応する取引履歴CSV等は、実際のエクスポートファイルを確認後にParser仕様を確定する

### 9.3 暗号資産オンチェーン取得

#### Bitcoin

- 公開アドレスを登録できること
- BTC残高を取得できること
- 送受信履歴を取得できる構造とする

#### Ethereum

- 公開アドレスを登録できること
- ETH残高を取得できること
- ETH送受信履歴を取得できること
- ERC-20等のトークン対応はMVP後または基本設計で範囲を確定する

#### Solana

- 公開アドレスを登録できること
- SOL残高を取得できること
- SOL送受信履歴を取得できること
- SPL Token対応はMVP後または基本設計で範囲を確定する

### 9.4 暗号資産取引所API

- 読み取り専用APIを前提にConnectorを追加可能な設計とする
- 初期対応取引所は別途決定する
- 対象取引所未決定のため、特定取引所API実装はMVP受入必須条件としない
- CSV取り込みを代替手段として残す

### 9.5 手動同期

- MVPでは同期開始はユーザ操作を基本とする
- アプリ起動中の定期同期はMVP後の拡張とする

---

## 10. クレジットカード要件

- 利用明細はCSV取り込みを基本とする
- カード利用日を保持する
- 請求月・引落月単位で請求額を管理する
- 未引落額を負債として確認できること
- 引落予定日・引落口座・引落済み/未引落状態を管理できること
- 銀行口座との完全自動照合はMVP後とする

主なデータ項目:

- `card_used_at`
- `billing_month`
- `payment_month`
- `payment_due_date`
- `withdrawal_account_id`
- `statement_status`
- `card_account_id`
- `merchant_name`
- `amount`

---

## 11. 実現損益要件

### 11.1 基本方針

- 税額計算ではなく実現損益の把握を目的とする
- 必要な取引データが不足している場合は、誤った値を確定表示せず不足状態を表示する
- 実現損益の根拠となる取引を確認できること

### 11.2 株式

- 初期対象は楽天証券で取得可能な株式取引データとする
- 日本株・米国株を対象候補とする
- `trade_date`、`settlement_date`、数量、単価、手数料等を保持できること
- 米国株等の外貨建て取引はJPY換算情報を保持できること
- 為替レートの取得元・優先順位は、楽天証券CSV等の実データ確認後に確定する

### 11.3 暗号資産

- BTC、ETH、SOLの取引について実現損益計算へ拡張可能な構造とする
- 暗号資産の取得単価計算は総平均法を基本とする
- オンチェーン残高のみでは取得価額を確定できない場合があることを前提とする
- 取引所CSV/API等から取得価額情報が不足している場合は、実現損益を「計算不可 / データ不足」と表示できること
- 自己ウォレット間移動は原則として損益イベントにしない

---

## 12. データ状態・鮮度・不足検知

### 12.1 目的

ダッシュボード上の数値が、古いデータや不足データに基づいていることをユーザが認識できるようにする。

### 12.2 管理する状態

少なくとも以下を区別する。

- 正常
- データが古い
- 必要データ未取り込み / 不足
- 取り込み失敗
- 同期失敗
- 競合あり
- 確認待ち

鮮度と完全性は別概念として扱う。

### 12.3 保持情報

取得元またはデータ種別ごとに、以下を保持できること。

- 最終成功取り込み日時
- 最終成功同期日時
- 最終試行日時
- 最終エラー
- 対象データ期間
- データ種別

### 12.4 ダッシュボード表示

- ダッシュボードでデータ状態を認識できること
- 古い、不足、失敗、競合等がある取得元を判別できること
- 必要に応じて取り込み・同期・競合確認画面へ移動できること
- **表示形式はMVP基本設計で決定し、TODO形式等には現時点で固定しない**

---

## 13. 画面要件（MVP）

最低限、以下の画面または同等の機能を提供する。

- 初回セットアップ
- ロック解除
- ダッシュボード
- 口座 / ウォレット一覧・詳細
- 取引 / 明細一覧
- CSV取り込み画面
- 同期画面・同期ログ
- 競合 / 確認待ち確認画面
- クレジットカード請求一覧
- 楽天証券保有資産表示
- 実現損益画面
- バックアップ / 復元
- 設定画面

ダッシュボードでは、少なくとも以下の概要を確認できること。

- 総資産
- 総負債
- 主要口座残高
- クレジットカード未引落額
- 証券保有資産
- 暗号資産残高
- データ状態

BS/PL形式の高度な会計表示はMVP必須とせず、上記統合ビューを優先する。

---

## 14. バックアップ・復元

### 14.1 バックアップ

- 暗号化DBをバックアップできること
- 任意の保存先を選択できること
- スキーマバージョン、アプリバージョン、作成日時等のメタ情報を保持すること

### 14.2 復元

- MVPでは**既存DBの全置換方式**とする
- 既存DBとのマージ復元はMVP対象外とする
- 復元前に既存DBを退避できること
- 古いスキーマのバックアップは必要なマイグレーションを実行できること
- 現アプリより新しいスキーマの場合は復元を拒否すること

### 14.3 認証情報

- APIキー等の認証情報はMVPバックアップへ含めない方針を基本とする
- 復元後は必要に応じて再入力する

---

## 15. DB・マイグレーション要件

- SQLAlchemyを利用可能な構成とする
- Alembic等でDBスキーマバージョンを管理する
- 原則として非破壊マイグレーションを優先する
- カラム追加はNULL許可またはデフォルト値を考慮する
- データ型変更や削除が必要な場合は、データ移行方法を明示する

---

## 16. MVP受入条件

以下を満たした時点をMVP完成の目安とする。

1. 初回起動時にパスフレーズを設定し、暗号化DBを作成できる
2. 起動時に毎回パスフレーズ入力を要求し、正しい場合のみDBへアクセスできる
3. パスフレーズなしではDB内容を実用的に読み取れない
4. 楽天銀行・楽天カード・沖縄銀行について、少なくとも対象として確定したCSV形式を取り込める
5. 楽天証券について、少なくとも保有残高CSVをSnapshotとして取り込み・表示できる
6. 楽天証券の取引履歴等、実現損益に必要な対応CSVを確定した場合、そのParserを追加できる構造になっている
7. 同じCSVを再取り込みしても二重登録されない
8. 未対応または変更されたCSV形式を推測して取り込まず、エラーとして認識できる
9. RawデータとNormalized Event / Snapshotを分けて保持できる
10. 取得元由来データとユーザ補正を分けて保持できる
11. 確度の高いIDによる重複判定と、不確実な一致の確認待ちを区別できる
12. クレジットカードの請求月・引落月単位の未引落額を確認できる
13. BTC、ETH、SOLの公開アドレスを登録でき、対象チェーンについて残高を取得できる
14. BTC、ETH、SOLについて送受信履歴を取得できる構造がある
15. ウォレットの秘密鍵・シードフレーズを扱わない
16. 暗号資産取引所APIを追加する場合、読み取り専用を前提とする
17. 株式・暗号資産の実現損益は、必要データが揃っている場合に表示し、不足している場合は不足状態を表示できる
18. ダッシュボードで資産・負債・主要残高・データ状態を確認できる
19. データが古い、不足、同期失敗、競合等の状態をユーザが認識できる
20. バックアップと全置換方式の復元ができる
21. `core/` と `infrastructure/` がPySide6のUI実装に依存しない
22. Connector / ParserをProvider × Data Type単位で追加可能な構造になっている

---

## 17. MVP後の拡張候補

- 楽天証券の追加CSV形式対応
- 他証券会社対応
- 銀行・カード系公式API対応
- 暗号資産取引所API対応
- Ethereum ERC-20対応
- Solana SPL Token対応
- アプリ起動中の定期同期
- Windowsタスクスケジューラ等による非起動時同期
- 残高照合と自動マッチング
- 高度な競合解決UI
- BS/PL・試算表等の高度な会計表示
- 税額参考表示
- 為替レート自動取得・補正
- DeFi / NFT / ステーキング / エアドロップ / ブリッジ対応
- AIによる分類・分析・補助機能
- モバイル対応

---

## 18. 未決事項

| 項目 | 状態 | 決定タイミング |
|---|---|---|
| PySide6 | 確定 | 済 |
| Windowsデスクトップ | 確定 | 済 |
| 初期証券会社 | 楽天証券に確定 | 済 |
| 楽天証券対応CSV種別 | 選定中 | Connector実装前 |
| 楽天銀行対応CSV形式 | 要実データ確認 | Connector実装前 |
| 楽天カード対応CSV形式 | 要実データ確認 | Connector実装前 |
| 沖縄銀行対応CSV形式 | 要実データ確認 | Connector実装前 |
| 初期暗号資産 | BTC / ETH / SOL | 済 |
| Bitcoin取得API / Provider | 未確定 | Crypto Connector設計時 |
| Ethereum取得API / Provider | 未確定 | Crypto Connector設計時 |
| Solana取得API / Provider | 未確定 | Crypto Connector設計時 |
| 初期暗号資産取引所API | 未確定 | MVP後またはCrypto設計時 |
| ERC-20対応範囲 | 未確定 | MVP後判断 |
| SPL Token対応範囲 | 未確定 | MVP後判断 |
| 為替レート取得・優先順位 | 未確定 | 楽天証券取引CSV確認後 |
| 実現損益の詳細計算規則 | 一部未確定 | 取引CSV確認後 |
| 暗号化SQLiteライブラリ | PoCで確定 | 実装初期 |
| KDFパラメータ | PoCで確定 | 実装初期 |
| データ状態のダッシュボード表示形式 | 未確定 | UI基本設計時 |
| Rawデータの具体保存形式 | 未確定 | DB基本設計時 |

---

## 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-06-01 | 初版作成 |
| 2026-06-04 | クレジットカードを請求月・引落月単位の管理へ変更。台帳・競合・整合性要件を追加 |
| 2026-08-13 | MVP作成に向けて全面整理。主目的を金融データの収集・決定論的正規化・統合へ再定義。AI正規化をMVP対象外と明記。初期証券会社を楽天証券、初期暗号資産をBTC/ETH/SOLに確定。EventとSnapshotを分離。データ鮮度・不足検知を追加。税額・給与所得・定期同期・高度な会計表示等をMVP後へ移動。MVP受入条件を再定義 |
