# 要件定義書（個人資産・カード管理アプリ）

| 項目 | 内容 |
|---|---|
| 文書名 | 要件定義書 |
| プロジェクト名 | 個人資産・カード管理アプリ（moneger） |
| 作成日 | 2026-06-01 |
| 最終更新日 | 2026-06-04 |
| 対象ユーザー | 作成者本人（自分用） |
| 対象OS | Windows |
| 開発言語 | Python |
| UIフレームワーク | PySide6（確定） |

---

## 1. 背景・目的

本アプリは、現金・銀行口座・クレジットカード・金融資産を一元管理し、資産/負債の状況を可視化することを目的とする。

特に、クレジットカード利用については、ユーザが用意したCSV等の明細をもとに、**請求月・引落月単位**で未引落額を把握し、引落口座との関係を含めて管理できることを重視する。

加えて、株式投資（日本株・米国株）および暗号資産の**実現損益**を把握し、税額の**概算（目安）**を表示できることを目的とする。

> 注: 本アプリは税務申告書の作成や提出を行うものではなく、税額は概算であり正確性を保証しない。

---

## 2. スコープ

### 2.1 スコープ内（本アプリで実現する）

- Windowsデスクトップアプリとして動作すること（Webアプリ不可）
- ローカル（オフライン）で資産・負債・収支を管理できること
- データの暗号化保存（PC盗難を想定）
- 楽天銀行／楽天カード／沖縄銀行のデータ取り込み（方式は後述）
- BS/PL等の会計的な可視化と、簡易UI（家計簿的UI）の両立
- 将来、暗号資産など新しい資産種類を追加可能な拡張性
- **株式（日本株・米国株）および暗号資産の実現損益を集計し、税額の概算（目安）を表示できること**
  - 暗号資産の損益計算方式は**総平均法**を採用する
  - 米国株等の外貨建て資産について、為替換算ルールを選択可能とする（→ 9.7）
  - 概算税額は資産種別（株式／暗号資産）で税率を分けて計算できること（→ 7.7, 9.8）
  - **概算税額に用いる税率はユーザが設定画面から変更できること**（→ 7.9, 9.8）

### 2.2 スコープ外（現時点では対象外）

- サーバ同期・複数端末間リアルタイム同期
- **iOS/Androidアプリのビルド・配布（将来検討）**
  - MVPはWindows専用とする
  - ただし、将来のモバイル対応可能性を保持するため、コアロジックとUI層を分離した設計を採用する（→ 5.6 参照）
- 第三者向け配布（インストーラ署名、ストア配布など）
- 税務申告書の自動生成・提出（e-Tax送信等）
- 確定申告の転記支援（税務様式に合わせた出力、提出用明細の生成等）
- Webスクレイピング/RPA等による金融機関サイトからの自動ログイン取得（原則不採用。採用可否は基本設計で判断）

---

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| 基軸通貨（Base Currency） | 集計・評価の基準通貨。本アプリではJPY固定（ただしコード変更で差し替え可能にする） |
| 月次請求管理（カード） | クレジットカード利用明細を請求月・引落月単位で集計し、未引落額を負債として把握する考え方 |
| 計上日/反映日 | 明細に記録される処理日・入出金日など（取得元により名称が異なる） |
| 取引（Transaction） | 入出金・支払い・振替・売買などの出来事 |
| 仕訳行（Leg） | 取引を構成する明細行（複式簿記を想定） |
| 取り込み（Import） | CSV等から明細を取得し、アプリ内データへ変換・登録する処理 |
| コネクタ（Connector） | 取得元（楽天銀行等）ごとの取り込み/取得処理モジュール |
| 正規化イベント（Normalized Event） | 取得元固有のデータを、アプリ共通の形式に変換したもの |
| KDF | パスフレーズから暗号鍵を導出する処理（Argon2id等） |
| スキーマバージョン | DBスキーマの版番号。Alembicで管理し、バックアップ復元時の互換性判定に使用する |
| 残高照合 | 銀行明細の「取引後残高（balance_after）」と、明細金額を積み上げた残高が一致するかを検証すること |
| 約定日 | 株式等で売買が成立した日付 |
| 受渡日 | 株式等で決済が完了した日付。年度判定等で利用される場合がある |
| 総平均法 | 期中の購入単価を総平均で算出し、売却損益を計算する方法（暗号資産の損益計算等） |
| 実現損益 | 売却等により確定した損益（未実現損益は含まない） |
| 概算税額 | 本アプリが便宜的な税率・簡略化した前提で算出する税額の目安（正確性は保証しない） |
| 給与所得 | ユーザが主たる勤務先から得る年間の給与収入。概算の累進税率を算出するための参考情報として利用する。 |

---

## 4. 前提・制約

### 4.1 前提

- 本アプリは「自分用」であり、利用者は1名を想定する
- ネットワーク接続は将来的に利用する可能性があるが、基本設計はオフラインで成立すること
- UIフレームワークにはPySide6を採用する。ただし、将来のモバイル対応可能性を保持するため、コア層とUI層を明確に分離すること（→ 5.6 参照）

### 4.2 制約

- アプリ種別はデスクトップアプリ（Webアプリ不可）
- 開発環境OSはWindows
- データはローカルに保存し、PC盗難・バックアップ流出を想定した暗号化が必要
- iOS向けビルドは将来検討事項であり、現フェーズでは実施しない（macOS + Xcodeが必要なため）

---

## 5. 全体要件（非機能要件含む）

### 5.1 セキュリティ要件（最重要）

- 保存データ（DBファイル）は**常時暗号化**されていること（暗号化SQLite（SQLCipher相当）または同等方式を必須とする）
- アプリ起動時に毎回、利用者は**パスフレーズを入力**して解除すること
- パスフレーズは**長いパスフレーズ**を想定し、**コピペを許可**すること
- 初回セットアップ時にパスフレーズを**2回入力して一致確認**すること
- パスフレーズ忘れ時は**復旧不可**（データ復号不可）でよい
- バックアップファイルの作成/復元時も**パスフレーズ入力を要求**すること（ログインと同一パスフレーズ）
- 暗号鍵は端末上に平文で永続保存しないこと（パスフレーズから都度導出する）
- クリップボード貼付の注意喚起を行うこと（履歴機能等に残る可能性）

> 注: 具体ライブラリ／ドライバはPoCで「作成→再起動→パスフレーズで再オープン→CRUD→バックアップ復元」が通るものを採用する。

### 5.2 可用性・信頼性

- 同一データを複数回取り込んでも重複登録されないこと（重複排除）
- 取り込み・集計・可視化は、途中で失敗した場合にエラー内容を提示し、DB整合性が保たれること

### 5.3 性能要件（暫定）

- データ件数が増えても致命的に操作不能にならないこと
  - 「多少の読み込み待ち」は許容する（快適さは最優先ではない）
- KDFによる解除時間は「標準的な設定」を採用し、極端な遅延にならない範囲で安全側に倒す
  - 初回セットアップ時または設定時にKDF実行時間を実測し、目標時間（例：0.5〜1.5秒）に収まる範囲で調整することを許容する（詳細は基本設計で確定）

### 5.4 保守性・拡張性

- 取得元（金融機関/カード）の追加、資産種類（法定通貨・株式・暗号資産等）の追加が可能な設計とする
- コア機能（暗号化DB、取引モデル、集計、可視化）と、取得元コネクタ（Connector）を分離する
- DBスキーマのバージョニングとマイグレーションを行えること（Alembicを使用）
- **非破壊マイグレーションポリシー**として、以下のルールを原則とする
  - カラムの削除は行わない（不要になったカラムはそのまま残す）
  - カラム追加時は必ずデフォルト値またはNULL許可とする
  - カラムのデータ型変更は行わない（必要な場合は新カラムを追加して移行する）

### 5.5 オフライン要件

- オフラインで、閲覧・集計・バックアップ/復元が可能であること
- データ取り込みはオフラインでも可能な方式（CSV等）を最低限サポートすること

### 5.6 アーキテクチャ方針（UI独立性）

将来のモバイル対応可能性を保持するため、以下の層分離を設計上の必須要件とする。
src/
├── core/            # PySide6依存ゼロ（ビジネスロジック・モデル・サービス）
│   ├── models/      #   Transaction, Account, Asset ...
│   ├── services/    #   ImportService, BalanceService, BackupService ...
│   ├── connectors/  #   RakutenBank, RakutenCard, OkinawaBank ...
│   └── ports/       #   UIが呼び出すインターフェース定義
├── infrastructure/  # DB・暗号化・ファイルI/O（UI非依存）
│   ├── db/          #   SQLAlchemy models, Alembicマイグレーション
│   └── crypto/      #   SQLCipher, KDF（Argon2id）
└── ui/              # PySide6はここのみに閉じ込める
    ├── windows/
    └── widgets/

**絶対に守るルール：`core/` および `infrastructure/` は `ui/` を import しない。**

この構造により、将来モバイル対応が必要になった場合は `ui/` 層のみを書き直すことで対応可能となり、コアロジック・DB・Connectorはそのまま再利用できる。

---

## 6. 対象データソース要件（取得・取り込み）

### 6.1 対象データソース

- 楽天銀行
- 楽天カード
- 沖縄銀行
- 証券口座（複数口座を想定）
- 暗号資産取引所（複数取引所を想定）
- 暗号資産ウォレット（複数ウォレット/アドレスを想定）

### 6.2 取得方式（要件としての方針）

- 利用者の手動入力は最小化する
- 取得方式は、利用者が取得元ごとに選択できる方式を基本とする
- 取得は「**公式API等による自動取得が可能であればそれを最優先**」とする
- APIが利用可能な取得元であっても、利用者の判断で**CSV等のファイルによる取り込みを選択**できること
- 以下のフォールバックも許容する
  - ユーザがWeb等から**CSV等の明細データをダウンロード**
  - アプリは当該ファイルを**取り込み（Import）**し、内部データへ変換・登録する
- 同期方式として、以下の両方を将来的に実装する
  - 手動同期（UIの同期ボタン）
  - 定期同期（アプリ起動中の定期実行、将来的にタスクスケジューラ等も検討）

### 6.3 取得方式の未確定事項（要決定）

楽天銀行／楽天カード／沖縄銀行等について、取得方式は「API優先・不可ならCSV等取り込み」を原則とし、取得元ごとに実現性を踏まえて基本設計フェーズで決定する。

| 優先度 | 方式 | 概要 | MVPでの扱い |
|---:|---|---|---|
| 1 | API（公式APIまたは同等の公認連携） | 認証・認可を経て明細を自動取得 | 可能なら採用（基本設計で判断） |
| 2 | CSV（明細ダウンロード取り込み） | ユーザが取得元サイト等から明細をダウンロードし、アプリが取り込む | **必須（保証）** |
| 3 | Webスクレイピング/RPA等 | 画面操作自動化による取得 | 原則不採用（規約・2FA・安定性の観点） |

> 本要件定義書では、MVPとして少なくとも「CSV取り込み」を必須要件として保証する。  
> 取得元ごとのAPI取得は「可能なら採用」とし、実現性・安全性・運用コストを基本設計で評価して決定する。

### 6.4 取り込みの共通要件

取得元ごとにConnectorを持ち、以下を実現する。

- 取得元データ（CSV/JSON等）を取り込み
- 正規化イベントへ変換
- 重複排除（source + external_id など、なければハッシュ）を行い、二重登録を防止
- 取り込み結果（件数、スキップ件数、エラー）をログ表示する
- 取得元データ取得手段（API/CSV）に依存せず、同一の正規化イベントへ変換し、コア処理を共通化する
- 銀行口座の将来的な残高照合に備え、取得元データに残高情報が存在する場合は、明細行の「取引後残高（`balance_after`）」を保存できること
- 日付は概念を分けて保持すること
  - `occurred_at`（発生日。取引が実際に発生した日付）
  - `posted_at`（計上日/反映日。口座残高に反映された日付）
  - クレジットカード利用については、利用日を明細属性として保持しつつ、負債額の管理は請求月・引落月単位で行えること（→ 7.4）
  - 株式等については `trade_date`（約定日）および `settlement_date`（受渡日）を保持できること（→ 9.6）
- 残高照合を行う場合に備え、取り込み時にCSV内の順序を保持できる情報（例：行番号 `row_index`、ファイル識別子等）を保存できること

> 注: 残高照合はMVPでは必須としないが、「照合できるデータを保持する」ことは要件として必須とする。

---

## 7. 機能要件

### 7.1 初回セットアップ

- DB未作成の場合、初回セットアップ画面を表示する
- パスフレーズを2回入力し一致した場合のみ、暗号化DBを作成する
- パスフレーズ忘れは復旧不可である旨を明示する

### 7.2 起動・ロック解除

- アプリ起動時はロック画面を表示し、パスフレーズ入力で解除する
- アプリロック解除後は、DBにアクセス可能となる
- パスフレーズ入力欄はマスクし、表示/非表示を切り替え可能とする
- コピペを許可する

### 7.3 資産・負債の管理（基本）

- 法定通貨（JPY）を管理できること
- 将来拡張として、株式・暗号資産などを追加できること

### 7.4 クレジットカード負債管理（月次請求単位）

- クレジットカード利用明細は、原則としてユーザが用意したCSV等を取り込んで登録する
- MVPでは、クレジットカード会社のAPIによるリアルタイム取得は前提としない
- カード利用日は明細情報として保持する
- 負債額の管理は、利用日単位ではなく、請求月・引落月単位を基本とする
- 請求月ごとの利用額を集計し、未引落分をクレジットカード負債として表示する
- 引落予定日、引落口座、引落済み/未引落の状態を管理できる設計とする
- 銀行口座明細との自動照合はMVP後の拡張とする

### 7.5 口座／ウォレット単位の管理（情報設計方針）

- 本アプリは、口座（銀行口座／証券口座／クレジットカード等）およびウォレット（暗号資産取引所口座／オンチェーンウォレット等）単位で管理する
- 口座／ウォレットはそれぞれ独立して閲覧できる（例：証券口座を複数持っている場合、口座ごとに独立したページ（ビュー）を持つ）
- 一方で、ホーム画面では全口座／全ウォレットを集約した一元ビューを提供する
  - 概要（簡易な財務情報）と詳細（財務諸表相当の詳細情報）を切り替えられること

> 注: 具体的な画面UIは基本設計で確定する。ここでは情報設計（口座粒度／集約粒度）の方針を要件として固定する。

### 7.6 取り込み（同期）

- **手動同期**
  - UI操作で取り込みを開始できる
- **定期同期**
  - アプリ起動中の定期実行をサポートする（間隔は設定可能を想定）
  - 将来、アプリ非起動時の同期（CLI + タスクスケジューラ等）も検討する
- **同期の動作モード**
  - 取得元がAPI取得とCSV取り込みの両方に対応する場合、ユーザはどちらを利用するか選択できること
  - API取得が利用可能な取得元については、同期処理はAPIからの自動取得を行う
  - API取得が利用できない取得元、またはユーザがCSV取り込みを選択した取得元については、同期処理は「ユーザが配置/指定したCSV等の取り込み」を行う
  - 取得元ごとに、利用可能な動作モード（API/CSV）をUI上で明示する

### 7.7 可視化・レポート

- 資産・負債バランスの可視化（BS）
- 収支の可視化（PL相当、期間集計）
- 収支・残高推移のグラフ表示
- **実現損益サマリ（年次/任意期間）**
  - 株式（日本株・米国株）および暗号資産の実現損益を表示できること
  - 口座／ウォレット単位および全体集約で表示できること
- **概算税額（目安）**
  - 実現損益をもとに概算税額を算出し、目安として表示できること
  - 概算税額は「株式」と「暗号資産」で税率を分けて計算できること
  - 概算税額の算出は簡略化した前提に基づき、正確性を保証しない旨をUI上に明示すること

### 7.8 バックアップ/復元（端末移行）

#### バックアップ作成

- パスフレーズ入力を要求し、正しいことを確認した上でバックアップを出力する
- 出力先はUSB/外付け/クラウドストレージ等、任意のパスを選択可能とする
- バックアップファイルには**スキーマバージョンをメタ情報として必ず記録**する
backup_20260601/
├── data.db       # 暗号化SQLite本体
└── meta.json     # {"schema_version": 3, "app_version": "1.0.0", "created_at": "..."}

#### 復元

- バックアップファイルを選択し、パスフレーズ入力で復元可能とする
- 復元時は既存DBを退避する等、破壊的操作の確認を行う（詳細は基本設計）
- **復元時のスキーマバージョン互換性チェックを行う**

| バックアップ vs 現アプリ | 動作 |
|---|---|
| バックアップが古い（スキーマ < 現在） | Alembicマイグレーションを自動実行して復元 |
| 同じバージョン | そのまま復元 |
| バックアップが新しい（スキーマ > 現在） | 「アプリを更新してください」とエラー表示し、復元しない |

### 7.9 設定（Preferences）

- アプリの設定画面を持つこと
- 設定画面から、概算税額に用いる税率をユーザが変更できること
  - 株式向け税率（概算）
  - 暗号資産向け税率（概算）
- 暗号資産の税額概算のため、ユーザが**給与所得（年収）を入力**できること
  - 入力された給与所得に基づき、所得税の累進課税を考慮した**推定限界税率の目安を自動計算**し、設定画面に表示すること
  - 表示された目安税率は参考情報であり、ユーザはこれを参考に税率を任意に上書きできること
- 税率の変更は、概算税額表示に即時反映されること（詳細は基本設計）

---

## 8. 画面要件（暫定）

### 8.1 必須画面（MVP）

- 初回セットアップ（DB作成、パスフレーズ2回入力）
- ロック解除（起動時パスフレーズ入力）
- メイン画面（ダッシュボード）
- 取引/明細一覧（台帳）
- 取り込み（同期）画面／同期ログ表示
- バックアップ作成／復元
- 設定画面（税率設定、給与所得入力等）
- （将来拡張）実現損益画面（年次/任意期間）

### 8.2 画面方針

- 「簡単な家計簿的UI」と「会計知識が必要なUI（財務諸表・勘定系）」の両方を実装する
- 口座／ウォレット単位のページ（ビュー）を持つ
- ホーム画面で一元集計し、概要↔詳細を切り替え可能にする
- 概算税額については「目安」であることを明示する
- 詳細なUIワイヤーフレームは基本設計で作成する

---

## 9. データ要件（暫定）

### 9.1 基軸通貨

- 基軸通貨は JPY とする
- 将来、ソースコード変更で差し替え可能な構造とする（設定ファイル/定数の一元化）

### 9.2 取引モデル（方向性）

- 取引（Transaction）と仕訳行（Leg）を中心に設計し、資産タイプ追加に耐えること
- 取得元の生データ（raw）も保存し、再現性・監査性を確保すること（保存可否・範囲は基本設計で確定）

### 9.3 スキーマ互換性方針

- スキーマ変更は非破壊マイグレーションポリシー（→ 5.4）に従う
- Alembicによりスキーマバージョンを管理し、upgrade/downgradeスクリプトを両方実装する
- downgradeスクリプトは、データ損失が生じる場合はその旨をコメントで明記する

### 9.4 明細の残高情報（残高照合の前提）

- 銀行明細について、取得元に残高列が存在する場合は、明細行ごとに `balance_after`（取引後残高）を保持できること
- 残高照合の実装に備え、以下の情報を保持できること（取得元に存在しない場合はNULL可）
  - `posted_at`
  - `balance_after`
  - `external_id`（取得元側のユニークID。存在しない場合はハッシュで代替）
  - `source_file_id`（どのファイル/API取得から来たかの識別子）
  - `row_index`（ファイル内の行番号）

### 9.5 照合の対象・単位（将来拡張）

- 残高照合は「口座（Account）単位」で実施できる設計とする
- 照合の対象期間を指定できる設計とする（例：CSV取り込み範囲、またはユーザ指定期間）

### 9.6 株式取引の日付要件（約定日・受渡日）

- 株式（日本株・米国株）について、取引データとして少なくとも以下の日付を保持できること
  - `trade_date`（約定日）
  - `settlement_date`（受渡日）
- 年度判定等で受渡日を利用できるようにすること（例：実現損益サマリの対象年の判定）

### 9.7 為替換算ルール（米国株等）

- 外貨建て資産（米国株等）の損益・評価額のJPY換算について、為替換算ルールを選択可能とする
  - 例：**約定日レート**／**受渡日レート**
- デフォルトは「**約定日レート**」とする
- 年度判定は受渡日を保持し、必要に応じて利用できるようにする（例：実現損益サマリの集計対象年の判定）

### 9.8 概算税額の前提（簡略化ルール）と税率設定

- 概算税額は、以下を前提として算出する
  - 株式と暗号資産で税率（概算）を分けて計算する
  - 所得控除、住民税、復興特別所得税、損益通算、繰越控除等は考慮しない（または基本設計で簡略化方針を定義する）
  - **暗号資産の税率（デフォルト設定なし）** は、ユーザが給与所得に基づく累進課税を参考に手動で設定することを想定する
- 概算税額は正確性を保証しない（目安）ことをUI上で明示する
- 税率（概算）はユーザ設定可能とし、以下を保持できること
  - `estimated_tax_rate_stock`（株式向け概算税率）
  - `estimated_tax_rate_crypto`（暗号資産向け概算税率）
- ユーザの給与所得（年収）を保持できること
  - `salary_income`（給与所得）

---

## 10. 運用要件

- 自分用運用のため、ユーザー管理（複数アカウント）は不要
- バックアップは利用者が適宜実施する（USB/外付け/クラウド）
- クラウド保管を想定し、バックアップファイル流出時も復号困難であることを前提に安全側のKDF設定とする

---

## 11. 未決事項・今後の検討項目

| 項目 | 状態 |
|---|---|
| UIフレームワーク | ✅ 確定（PySide6） |
| 対象プラットフォーム（MVP） | ✅ 確定（Windowsデスクトップ専用） |
| iOS/Android対応 | 将来検討（コア/UI分離設計で可能性を保持） |
| 楽天銀行／楽天カード／沖縄銀行の取得方式 | 未確定（**API優先・不可ならCSV。ユーザがAPI/CSVを選択可能**。基本設計フェーズで取得元ごとに決定） |
| 各取得元のデータ形式（CSVヘッダ、日付、金額符号、ユニークID有無） | 未確定 |
| 同期の定期実行方式（アプリ起動中のみか、タスクスケジューラ連携までMVPに含めるか） | 未確定 |
| 具体的な暗号化方式（暗号化SQLiteの採用可否、ライブラリ、配布形態） | 未確定（PoC検証後に確定） |
| KDFパラメータの決定（実測調整の有無） | 未確定 |
| 会計UI（BS/PL/試算表）と簡易UIの具体仕様 | 未確定 |
| カード引落シミュレーションの詳細仕様 | 未確定 |
| 自動分類ルール（摘要/店名からのカテゴリ推定等）の採用可否 | 未確定 |
| 価格取得（株価/暗号資産/為替）の導入タイミング | 未確定（将来拡張） |
| 残高照合（銀行明細の積み上げと残高の一致検証） | 将来拡張（**データ保持は要件で必須**。UI/判定ロジックは基本設計で定義） |
| 概算税額に用いる税率のデフォルト値（株式／暗号資産） | 未確定（基本設計で定義） |
| 株式の対象範囲（ETF/投信/ADR等を含むか） | 未確定 |
| 配当・分配金を実現損益・概算税額に含めるか | 未確定 |
| 為替レートの取得元・保持方式（API/CSV/手入力、レートテーブル設計） | 未確定 |

---

## 12. 受入条件（暫定）

MVPの受入条件として、最低限以下を満たすこと。

1. 初回セットアップでパスフレーズを設定し、暗号化DBが作成できる
2. アプリ起動時に毎回パスフレーズ入力が必要で、正しい場合のみデータにアクセスできる
3. 暗号化DBファイルを外部にコピーしても、パスフレーズなしでは内容が読み取れない
4. 取り込みにより明細を登録できること（**API自動取得が利用可能な場合でも、ユーザの選択によりAPIまたはCSV取り込みのいずれでも登録できること**）
5. 同じ明細を再取り込みしても重複登録されない
6. クレジットカード利用明細をCSV等で取り込み、請求月・引落月単位で未引落額をクレジットカード負債として確認できること
7. BS/収支等の基本可視化ができる（簡易でよい）
8. バックアップ作成・復元ができ、USB/外付け/クラウド任意パスに保存できる
9. 復元時にスキーマバージョンを確認し、古いバックアップは自動マイグレーション、新しいバックアップはエラーとなること
10. 取得元追加・資産種類追加を想定した構造（Connector分離、取引中心モデル、スキーマバージョン管理）がある
11. `core/` および `infrastructure/` が `ui/` に依存していないこと（UI独立性の検証）
12. 銀行明細取り込みで残高列が存在する場合、`balance_after` と `posted_at` を保持できること（存在しない場合はNULLでも動作すること）
13. 明細は `occurred_at` と `posted_at` を概念として分けて保持し、用途（家計簿的集計／残高推移）に応じて利用できること
14. 株式（日本株・米国株）の取引で `trade_date`（約定日）と `settlement_date`（受渡日）を保持できること
15. 外貨建て資産のJPY換算ルールを「約定日レート／受渡日レート」等から選択でき、デフォルトが約定日レートであること
16. 株式および暗号資産の実現損益を年次または任意期間で集計し表示できること
17. 概算税額を「株式」と「暗号資産」で税率を分けて算出し、目安として表示できること（正確性は保証しない旨を表示）
18. 設定画面で税率（株式向け／暗号資産向け）を変更でき、概算税額表示に反映されること
19. 設定画面で給与所得を入力し、参考となる推定限界税率の目安を確認できること

---

## 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-06-01 | 初版作成 |
| 2026-06-01 | UIフレームワークをPySide6に確定。5.6（アーキテクチャ方針）を追加。7.7にスキーマバージョン互換性要件を追加。5.4に非破壊マイグレーションポリシーを追加。9.3（スキーマ互換性方針）を追加。11（未決事項）を状態付きテーブルに更新 |
| 2026-06-01 | 取得方針を「**API優先・不可ならCSV等ダウンロード取り込み**」に更新（6.2/6.3/7.6/11/12を更新）。5.1で暗号化DB方式を必須化し、5.3にKDF実測調整の方針を追記。 |
| 2026-06-01 | 将来の残高照合に備え、`balance_after` / `posted_at` 等の保持要件を追加（6.4、9.4、9.5、11、12を更新）。 |
| 2026-06-01 | 口座/ウォレット単位管理方針、株式の約定日/受渡日保持、為替換算ルール選択（デフォルト約定日）を追加（6、7、8、9、11、12を更新）。 |
| 2026-06-01 | 税務要件を「確定申告支援」から「実現損益＋概算税額（株式/暗号資産で税率分離）」へ縮小（1、2、3、7.7、9.8、11、12を更新）。 |
| 2026-06-01 | 概算税額の税率をユーザが設定画面で変更できる要件を追加（2.1、7.9、8.1、9.8、11、12を更新）。 |
| 2026-06-02 | 銀行APIの取得方式について、API利用時でもユーザがCSV取り込みを選択できる「選択制」であることを明記（6.2、6.3、7.6、11、12を更新）。 |
| 2026-06-02 | 暗号資産の税率設定に関連し、給与所得入力機能とそれに基づく推定限界税率表示の要件を追加。用語定義に「給与所得」を追加（3、7.9、9.8、11、12を更新）。 |
| 2026-06-04 | クレジットカード負債管理について、発生主義（利用日基準）から、CSV取り込みを前提とした請求月・引落月単位の月次請求管理へ方針を変更（1、3、6.4、7.4、12を更新）。 |
